# Images

Project media, one subdirectory per project slug. Example: `images/gameboard/`.

Referenced from `projects/<slug>.html` as `../images/<slug>/<file>` and from `index.html` as `images/<slug>/<file>`.
